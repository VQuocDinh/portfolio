/** CV file in `public/` — URL basename = default save name (browsers often ignore `download` for PDFs). */
export const CV_FILENAME = 'Fullstack_VoQuocDinh.pdf';
export const CV_HREF = `/${CV_FILENAME}`;
/** Kept for anchors; filename is primarily enforced by the public URL above */
export const CV_DOWNLOAD_NAME = CV_FILENAME;
